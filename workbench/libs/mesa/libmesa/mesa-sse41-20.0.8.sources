# Exact SSE4.1 inventory from Mesa 20.0.8. Keep the headers in the upstream
# variable; the link-library recipe selects its two C translation units.
X86_SSE41_FILES := \
	main/streaming-load-memcpy.c \
	main/streaming-load-memcpy.h \
	main/sse_minmax.c \
	main/sse_minmax.h
